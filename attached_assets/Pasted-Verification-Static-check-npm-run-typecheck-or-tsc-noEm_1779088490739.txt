Verification
Static check: npm run typecheck (or tsc --noEmit) passes.
Build: npm run build succeeds.
Manual smoke test — Standalone builder:
Navigate to /story (direct URL load — confirms Router wiring) → Build tab shows 5-C form + live preview.
Fill title + 5 fields; preview updates each keystroke; copy buttons show toast and write to clipboard.
Save → toast confirms, form clears, switches to Vault; card appears with formatted date.
Search filters by title/character/context.
Edit pencil → loads card back into Build form; Save now updates instead of inserting.
Delete trash → window.confirm prompts; cancel keeps card, confirm removes.
Refresh page → vault persists from localStorage under key restless_story_vault_v01.
Empty state on first visit shows "Vault is Empty" copy.
Manual smoke test — Roleplay integration:
Save a story in the vault.
Navigate to Roleplay → setup phase shows a "Load Story Context" button.
Picker shows the saved story; selecting it shows "Story: <title>" badge.
Start the roleplay → confirm the system prompt sent to the LLM contains the buyer-background block derived from the story (inspect via dev tools network or temporary console log).
Clear story → badge disappears; subsequent prompts omit the block.
Manual smoke test — Lane integration:
On a fresh install, navigate to Lane Plan → existing scoring works unchanged (forward compat with v01 storage).
"Link Story" on a lane opens picker; selection persists across refresh.
"Copy 15s" copies the script for the linked story; toast confirms.
"Unlink" clears the link.
Live-sync check (cross-tab): Open Builder and Roleplay in two browser tabs (same origin). Add a story in Builder → Roleplay's picker should reflect the new story automatically via the storage event subscription in StoryVaultProvider. No refresh required.
Cross-browser clipboard fallback: force a non-secure-context test (or stub navigator.clipboard = undefined); confirm the try/catch path fires and toast shows 'Copy failed — long-press to copy manually'.
Deleted-story link resilience: Link a story to a lane, then delete it from the Builder. Reload Lane Plan → the lane should render with a faint "Linked story removed" caption and an Unlink action; nothing should throw.
Critical files (for reference during execution)
client/src/pages/NetworkTracker.tsx:15-29, 39-41 — CRUD-on-localStorage pattern to mirror in lib/storyVault.ts
client/src/pages/ClaimScanner.tsx — single-file tool page convention with inline styles
client/src/pages/Home.tsx:20, 22-26, 30-37, 39-41, 110-155, 167-182 — exact insertion points + tab styling precedent
client/src/pages/RoleplaySimulator.tsx:561, 704-720, 737, 754 — system prompt assembler and setup-phase state to integrate against
client/src/pages/LaneSelector.tsx:13-19, 75, 96-107, 219-262 — type definition, storage key, save/load, mutator
client/src/App.tsx:23-35, 38-48 — Router Switch (add /story) and provider chain (insert StoryVaultProvider)
client/src/components/FieldManual/tools/Tool10StoryVault.tsx — framework reference; the new builder is its interactive companion
client/src/contexts/AppState.tsx — useToastActions source